name: domain_all
description: 领域层模块（全部 UseCase：规则评估/文案获取/反馈记录/降频计算）
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_health:
    path: ../core_health
  core_model:
    path: ../core_model
  core_network:
    path: ../core_network
  base_utils:
    path: ../base_utils
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  base_lints:
    path: ../base_lints
  build_runner: ^2.15.1
  injectable_generator: ^3.1.3
