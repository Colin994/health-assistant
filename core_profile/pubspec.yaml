name: core_profile
description: 画像域数据层（健康标签、静音时段、频控参数；W2+ 落地）
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_model:
    path: ../core_model

dev_dependencies:
  base_lints:
    path: ../base_lints
