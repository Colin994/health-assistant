name: feature_onboarding
description: 首启引导模块（价值页/健康标签/权限引导，设计稿 01–03）
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  feature_design_system:
    path: ../feature_design_system
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  base_lints:
    path: ../base_lints
  build_runner: ^2.15.1
  injectable_generator: ^3.1.3
