name: core_reminder
description: 提醒域数据层（规则配置、触发状态、advice 日志、反馈事件；W2 落地）
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_model:
    path: ../core_model

dev_dependencies:
  base_lints:
    path: ../base_lints
